import { useState } from "react";
import { motion } from "framer-motion";

const Sidebar =()=>{
    return(
        <section className="flex flex-col  h-[100vh] w-[30vw] right-0 bg-[var(--bg-tertiary)] border-l-4 border-[var(--brand-secondary)] ">
            test
        </section>
    )
}

export default Sidebar;